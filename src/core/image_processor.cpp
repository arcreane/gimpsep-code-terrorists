#include "image_processor.hpp"

namespace ai_slop {
    // The base class implementation is empty since all methods are either
    // pure virtual or have default implementations in the header
} // namespace ai_slop 